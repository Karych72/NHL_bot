# План работ — 2026-09-12 (остаток)

**Это срез, а не замена.** Полные формулировки, критерии приёмки и команды проверки
остаются в [`work_plan_2026-08-08.md`](./work_plan_2026-08-08.md) — единственном источнике
истины по задачам. Здесь для каждой оставшейся задачи: что это, насколько нужно и на что
влияет, чтобы выбирать следующую задачу не перечитывая 900 строк. Нумерация сквозная с
исходным планом — номер задачи здесь и там один и тот же.

**Состояние на 2026-09-12.** `origin/master` = `6bfe826` (merge PR #20). Из 31 задачи закрыто
15, одна (29) сделана наполовину, остаток — 16 задач ниже. Задача 13 закрыта PR #20:
два мультисезонных теста починены, `make test-db-data` зелёный (7/7), отчёт по SQL оформлен.
В БД 5 сезонов × 1312 игр (6560), поля Задачи 28 заполнены — проверено на живой БД.
Датасет при этом однолетний: `artifacts/datasets/season_20252026/` — 1211 игр, собран
2026-04-27, `features_hash b334df68…`. Загрузка в БД ≠ пересобранный датасет.

**Шкала надобности:** 🔴 без неё стоит целый трек или в мастере остаётся неверное
поведение · 🟠 закрывает подтверждённый долг или риск · 🟡 качество и удобство, можно
брать в окно между крупными · ⚪ можно не делать в этом цикле.

## Сводка

| # | Задача | Что это | Надобность | На что влияет |
|---|---|---|---|---|
| 13 | Загрузка 3–5 сезонов ✅ | Выполнена (PR #20): тесты БД переведены на мультисезонные инварианты, отчёт по SQL оформлен | — | Снят блокер Задачи 29: `make test-db-data` зелёный |
| 29 | Перезагрузка 5 сезонов 🟨 | Данные перезагружены 23.08; остаток — сверка M10(a) по буллитам | 🔴 | Разблокирует 18 и 24; без сверки M10(a) `is_shootouts` может быть повально `false` на 2021–2024 |
| 30 | Пересобрать датасет на 5 сезонах | Прогон `build-dataset` по всем сезонам + сброс rolling-окон на границе сезона | 🔴 | Единственный вход в трек B: блокирует 14, 15, 21 и через них 22, 26 |
| 14 | Параметры сплита | Профиль walk-forward под 6.5k игр + guard на малый N | 🟠 (после 30) | Без неё обучение на мультисезоне идёт с конфигом под один сезон |
| 15 | Инференс-контур | Загрузка модели по `latest`, сверка `features_hash`, predict-датасет | 🟠 (после 30) | Инференс ни разу не прогонялся; блокирует 22, через неё 26 |
| 16 | Docstrings бота | 43 публичные функции без docstring + 4 модульные шапки | 🟡 | Читаемость `telegram_bot/`; пересекается с 27 (судьбу `close_pool` решить до начала) |
| 18 | «Спящие» данные | Вывести в боте уже загружаемые `shifts`, `time_on_ice` | 🟠 (после 29) | Видимая польза пользователю из данных, за которые уже заплачено загрузкой |
| 19 | Миграции с rollback | Нумерованные up/down либо Alembic; каталог `data_tables/migrations/` пуст | 🟠 | Любое изменение DDL сейчас необратимо. Внутри — предусловие по FK: `supplement_rosters_from_reports` не добавляет игроков из shottype-отчёта, с FK это обрушит транзакцию загрузки сезона |
| 20 | Hardening Docker | `USER` и `HEALTHCHECK` в `Dockerfile`; решить судьбу `POSTGRES_HOST_AUTH_METHOD: trust` | 🟠 | Бот работает от root. Задача маленькая и ни от чего не зависит — хороший заполнитель окна |
| 21 | Олимпийская пауза | Флаг разрыва для rolling-фич внутри сезона (74 игры, февраль 2026) | 🟡 (после 30) | Сужена: механизм сброса окон приходит из Задачи 30, здесь только разрыв внутри сезона |
| 22 | Прогноз в боте | Строка прогноза в карточке игры, только после acceptance-gate | 🟡 (после 14, 15) | Единственная пользовательская отдача трека B; выпускать раньше gate нельзя — прогноз неотличим от base-rate |
| 23 | Три звезды матча | Новый сбор из API + хранение + вывод — данных нет нигде в проекте | 🟠 (после 31) | Единственная задача остатка, требующая нового прогона по всем 5 сезонам (~13k запросов). По объёму это уровень трека A, а не P3-правка бота |
| 31 | Кэш сырых пер-игровых ответов | Класть `play-by-play`/`boxscore` финальных игр на диск и читать оттуда | 🟠 (до 23) | Снимает цену будущих правок парсера: «добавить поле из pbp» перестаёт стоить 13k запросов. Сезонные отчёты не кэширует — они пересчитываются постоянно |
| 24 | Серии W/L | Вычисляемые фичи поверх формы команды | ⚪ (после 29) | Форма зависит от `is_shootouts`; до закрытия 29 строка В–П–ПО врёт. Прогона **не** требует — считается поверх уже лежащих `games` |
| 25 | Lock-файл + mypy | `uv`/`pip-tools` lock; сузить `ignore_missing_imports` | 🟡 | Сборка невоспроизводима: три `requirements*.txt` без пинов транзитивных зависимостей; mypy глобально слеп к типам библиотек |
| 26 | Retrain через cron | pipeline → train → gate → сдвиг `latest` | ⚪ (после 22) | Последнее звено трека B; до него весь цикл ручной |
| 27 | Чистка мёртвого кода | `close_pool`, `get_goal_video_url`, индекс `plan/README.md` | 🟡 | `plan/README.md` не упоминает ни одного `work_plan_*` — планы не находятся из индекса. Решение по `close_pool` нужно до Задачи 16 |

## Порядок

Одна очередь, сверху вниз — так и брать.

| № | Задача | Почему здесь |
|---|---|---|
| 1 | **29** Сверка M10(a) | 🔴 Остаток — один SQL и отметка. Разблокирует 18 и 24 |
| 2 | **30** Пересборка датасета | 🔴 Вход в трек B: без неё стоят 14, 15, 21, 22, 26 |
| 3 | **31** Кэш сырых ответов | Строго до 23, иначе её прогон потратит ~13k запросов впустую |
| 4 | **19** Миграции + предусловие FK | Строго до 23: снимает риск обвала транзакции на прогоне |
| 5 | **23** Три звезды | Прогон по 5 сезонам. Только после 31 и 19 |
| 6 | **18** «Спящие» данные | Открывается закрытием 29 |
| 7 | **14** Параметры сплита | Трек B, после 30 |
| 8 | **15** Инференс-контур | Трек B, после 14 |
| 9 | **21** Олимпийская пауза | Трек B, после 30 |
| 10 | **22** Прогноз в боте | Трек B, после 14 и 15 |
| 11 | **26** Retrain через cron | Трек B, последнее звено, после 22 |
| 12 | **27** Чистка мёртвого кода | Решение по `close_pool` нужно принять до 16 |
| 13 | **16** Docstrings бота | После 27 |

**Вне очереди — заполнять окна между крупными:** 20 (Docker, маленькая и ни от чего не
зависит), 25 (lock + mypy), 24 (серии W/L — после 29).

**Жёстких зависимостей всего четыре:** 30 → трек B · 31 → 23 · 19 → 23 · 27 → 16.
Остальное в таблице — приоритет, а не блокировка: местами можно менять.

**Прогон загрузчика нужен ровно одной задаче — 23.** Не путать: 24 считается поверх уже
лежащих `games`, 18 выводит `shifts` из сезонного отчёта, уже загруженного в БД.

## Регламент

Не дублируется: обязательные команды, бюджет задачи, правила отметки о закрытии и
Global Constraints — в [`CLAUDE.md`](../../CLAUDE.md) и в шапке
[`work_plan_2026-08-08.md`](./work_plan_2026-08-08.md). Отметка о закрытии задачи ставится
**в исходном плане 2026-08-08** (три места: заголовок, строка статуса, сводка) и
дополнительно — в таблице этого файла.
