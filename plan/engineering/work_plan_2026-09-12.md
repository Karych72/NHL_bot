# План работ — 2026-09-12 (остаток)

**Это срез, а не замена.** Полные формулировки, критерии приёмки и команды проверки
остаются в [`work_plan_2026-08-08.md`](./work_plan_2026-08-08.md) — единственном источнике
истины по задачам. Здесь для каждой оставшейся задачи: что это, насколько нужно и на что
влияет, чтобы выбирать следующую задачу не перечитывая 900 строк. Нумерация сквозная с
исходным планом — номер задачи здесь и там один и тот же.

**Состояние на 2026-09-13.** `origin/master` = `fa8f936` (merge PR #22, Задача 16). Из 30
задач закрыто 17, одна (29) сделана наполовину, остаток — 13 задач ниже. Задача 13 закрыта
PR #20: два мультисезонных теста починены, `make test-db-data` зелёный (7/7), отчёт по SQL
оформлен. В БД 5 сезонов × 1312 игр (6560), поля Задачи 28 заполнены — проверено на живой
БД. Задача 16 закрыта PR #22 (43 функции и 4 шапки, охват проверен скриптом). Задача 30
закрыта веткой `sdd-task-30-dataset` (PR — проставит человек): датасет пересобран на всех
5 сезонах — `artifacts/datasets/dataset_train.csv`, `dataset_rows: 6049`,
`features_hash: 09f1fac8…` (новый; старый `b334df68…` больше не актуален). Разница с 6560 —
511 строк, отброшенных cold-start policy на границе каждого сезона (ожидаемое следствие
фикса, не регресс). Загрузка в БД ≠ пересобранный датасет — это больше не расхождение.

**Шкала надобности:** 🔴 без неё стоит целый трек или в мастере остаётся неверное
поведение · 🟠 закрывает подтверждённый долг или риск · 🟡 качество и удобство, можно
брать в окно между крупными · ⚪ можно не делать в этом цикле.

## Сводка

| # | Задача | Что это | Надобность | На что влияет |
|---|---|---|---|---|
| 13 | Загрузка 3–5 сезонов ✅ | Выполнена (PR #20): тесты БД переведены на мультисезонные инварианты, отчёт по SQL оформлен | — | Снят блокер Задачи 29: `make test-db-data` зелёный |
| 29 | Перезагрузка 5 сезонов 🟨 | Данные перезагружены 23.08; остаток — сверка M10(a) по буллитам | 🔴 | Разблокирует 18 и 24; без сверки M10(a) `is_shootouts` может быть повально `false` на 2021–2024 |
| 30 | Пересобрать датасет на 5 сезонах ✅ | Выполнена (ветка `sdd-task-30-dataset`): датасет пересобран на всех сезонах, 6049 строк, сброс rolling-окон на границе сезона | — | Снят единственный блокер трека B: 14, 15, 21 разблокированы |
| 14 | Параметры сплита | Профиль walk-forward под 6.5k игр + guard на малый N | 🟠 | Без неё обучение на мультисезоне идёт с конфигом под один сезон |
| 15 | Инференс-контур | Загрузка модели по `latest`, сверка `features_hash`, predict-датасет | 🟠 | Инференс ни разу не прогонялся; блокирует 22, через неё 26 |
| 16 | Docstrings бота ✅ | Выполнена (PR #22): 43 функции и 4 шапки закрыты, охват проверен скриптом | — | Читаемость `telegram_bot/` |
| 18 | «Спящие» данные | Вывести в боте уже загружаемые `shifts`, `time_on_ice` | 🟠 (после 29) | Видимая польза пользователю из данных, за которые уже заплачено загрузкой |
| 19 | Миграции с rollback | Нумерованные up/down либо Alembic; каталог `data_tables/migrations/` пуст | 🟠 | Любое изменение DDL сейчас необратимо. Внутри — предусловие по FK: `supplement_rosters_from_reports` не добавляет игроков из shottype-отчёта, с FK это обрушит транзакцию загрузки сезона |
| 20 | Hardening Docker | `USER` и `HEALTHCHECK` в `Dockerfile`; решить судьбу `POSTGRES_HOST_AUTH_METHOD: trust` | 🟠 | Бот работает от root. Задача маленькая и ни от чего не зависит — хороший заполнитель окна |
| 21 | Олимпийская пауза | Флаг разрыва для rolling-фич внутри сезона (74 игры, февраль 2026) | 🟡 | Сужена: механизм сброса окон пришёл из Задачи 30, здесь только разрыв внутри сезона |
| 22 | Прогноз в боте | Строка прогноза в карточке игры, только после acceptance-gate | 🟡 (после 14, 15) | Единственная пользовательская отдача трека B; выпускать раньше gate нельзя — прогноз неотличим от base-rate |
| 23 | Три звезды матча | Новый сбор из API + хранение + вывод — данных нет нигде в проекте | ⚪ | По объёму это задача уровня трека A, а не P3-правка бота. Не брать как «мелочь» |
| 24 | Серии W/L | Вычисляемые фичи поверх формы команды | ⚪ (после 29) | Форма зависит от `is_shootouts`; до закрытия 29 строка В–П–ПО врёт |
| 25 | Lock-файл + mypy | `uv`/`pip-tools` lock; сузить `ignore_missing_imports` | 🟡 | Сборка невоспроизводима: три `requirements*.txt` без пинов транзитивных зависимостей; mypy глобально слеп к типам библиотек |
| 26 | Retrain через cron | pipeline → train → gate → сдвиг `latest` | ⚪ (после 22) | Последнее звено трека B; до него весь цикл ручной |
| 27 | Чистка мёртвого кода | `close_pool`, `get_goal_video_url`, индекс `plan/README.md` | 🟡 | `plan/README.md` не упоминает ни одного `work_plan_*` — планы не находятся из индекса. Задача 16 закрыта без ожидания этого решения (docstring на `close_pool` есть, судьбу функции решает эта задача) |

## Порядок

**Ближайшая — разблокировка** (Задачи 13, 16 и 30 из очереди ушли, закрыты PR #20, PR #22
и веткой `sdd-task-30-dataset`):

1. **Задача 29** — сверка M10(a): количество игр с `is_shootouts = true` по каждому сезону
   против публичной статистики NHL, а не «код не упал». Затем отметка о закрытии.
   Второй критерий, зелёный `make test-db-data`, уже снят Задачей 13.

**Дальше две ветки, порядок между ними свободный:**

- Трек B (вход открыт Задачей 30): 14 → 15 → 21 → 22 → 26.
- Трек A: 19 и 18 (18 — сразу после 29), затем 27, 24 — когда закрыта 29.

**Не зависят ни от чего — брать в любое окно:** 20, 25, 27.
**Пересмотреть перед взятием:** 23 — по объёму это не P3.

## Регламент

Не дублируется: обязательные команды, бюджет задачи, правила отметки о закрытии и
Global Constraints — в [`CLAUDE.md`](../../CLAUDE.md) и в шапке
[`work_plan_2026-08-08.md`](./work_plan_2026-08-08.md). Отметка о закрытии задачи ставится
**в исходном плане 2026-08-08** (три места: заголовок, строка статуса, сводка) и
дополнительно — в таблице этого файла.
