# План работ — 2026-09-12 (остаток)

**Это срез, а не замена.** Полные формулировки, критерии приёмки и команды проверки
остаются в [`work_plan_2026-08-08.md`](./work_plan_2026-08-08.md) — единственном источнике
истины по задачам. Здесь для каждой оставшейся задачи: что это, насколько нужно и на что
влияет, чтобы выбирать следующую задачу не перечитывая 900 строк. Нумерация сквозная с
исходным планом — номер задачи здесь и там один и тот же.

**Состояние на 2026-09-15.** `origin/master` = `b42c8ab` (merge PR #30, снятие дрейфа после
PR #26). Из 32 задач закрыто 24, остаток — 8 задач ниже. Задача 23 закрыта (ветка
`sdd-task-23-three-stars`, PR #31) — в мастере её ещё нет. Задача 14 закрыта PR #26: слепые нижние
границы `SplitConfig` опущены до санитарных, защиту взял guard `_check_minimum_history` —
он считает минимум строк под фактическую геометрию сплита и падает с обоими числами
(требуется / есть); рядом с рабочим `configs/modeling_default.yaml` (300/300/5) появился
smoke-профиль `configs/modeling_smoke.yaml` (50/50/3). Задача 32 закрыта PR #27:
`_assert_strictly_before` сравнивает позиции в отсортированной ленте, а не календарные дни —
на реальном календаре из БД (895 игровых дней, 6560 матчей) сплит строится обоими профилями,
блокер Задачи 15 снят; заведена она 2026-09-14 при ревью Задачи 14, и её номер —
идентификатор, а не позиция в очереди. Задача 20 закрыта PR #28: non-root `USER` и
`HEALTHCHECK` в `Dockerfile`, порт `db` убран на loopback. Задача 19 закрыта PR #29: механизм
миграций с откатом (`data_tables/migrations/NNNN_slug.up/down.sql`, учёт в
`schema_migrations`, цели `db-migrate`/`db-migrate-down` внутри `db-sync`/`db-reset`) и
предусловие FK в загрузчике. Задача 31 закрыта PR #25: диск-кэш сырых
пер-игровых ответов NHL API реализован и измерен на живой БД — блокер Задачи 23 снят.
Задача 13 закрыта PR #20: два мультисезонных
теста починены, `make test-db-data` зелёный (7/7), отчёт по SQL оформлен. Задача 16 закрыта
PR #22: 43 публичные функции и 4 модульные шапки `telegram_bot/` задокументированы, охват
проверен скриптом. Задача 29 закрыта PR #23: сверка M10(a) с официальными standings NHL
дала нулевую дельту во всех пяти сезонах — блокер Задач 18 и 24 снят. Задача 30 закрыта
PR #24: датасет пересобран на всех 5 сезонах — `artifacts/datasets/dataset_train.csv`,
`dataset_rows: 6049`, `features_hash: 09f1fac8…` (новый; старый `b334df68…` больше не
актуален); разница с 6560 — 511 строк, отброшенных cold-start policy на границе каждого
сезона (ожидаемое следствие фикса, не регресс) — блокер Задач 14, 15 и 21 снят.
В БД 5 сезонов × 1312 игр (6560), поля Задачи 28 заполнены — проверено на живой БД.
Загрузка в БД ≠ пересобранный датасет — но это больше не расхождение.
Задача 23 закрыта (ветка `sdd-task-23-three-stars`, PR #31): таблица `game_three_stars`
и третий пер-игровой эндпоинт `gamecenter/{id}/landing` (звёзд нет в `boxscore`) добавлены
в диск-кэш Задачи 31, бот выводит блок «Звёзды матча» в карточке игры. Прогон по всем
5 сезонам и проверка на живой БД выполнены координатором 2026-09-15…09-18 (6560 игр):
`game_three_stars` — 19 670 строк (дефицит 10 от 3 × 6560 — 8 игр, где NHL API отдаёт
меньше трёх звёзд, свойство данных, не дефект разбора), целостность и `make test-db-data`
(10 тестов) зелёные; диск-кэш `all_data/raw` заполнен до 132 МБ (19 680 файлов), повторный
прогон по этим сезонам бесплатен.
Обновлено 2026-09-19: Задача 18 закрыта (ветка `sdd-task-18-sleeping-data`, смерджена в master локально, без PR) — хвост
строки лидербордов команд/игроков/вратарей в `bot_messages.py`, сортировка не менялась.
Следующая по порядку — Задача 15 (вход в трек B).

**Шкала надобности:** 🔴 без неё стоит целый трек или в мастере остаётся неверное
поведение · 🟠 закрывает подтверждённый долг или риск · 🟡 качество и удобство, можно
брать в окно между крупными · ⚪ можно не делать в этом цикле.

## Сводка

| # | Задача | Что это | Надобность | На что влияет |
|---|---|---|---|---|
| 13 | Загрузка 3–5 сезонов ✅ | Выполнена (PR #20): тесты БД переведены на мультисезонные инварианты, отчёт по SQL оформлен | — | Снят блокер Задачи 29: `make test-db-data` зелёный |
| 29 | Перезагрузка 5 сезонов ✅ | Выполнена (PR #23): сверка M10(a) прошла — числа буллитных игр совпали с официальными standings NHL во всех 5 сезонах, дельта 0 | — | Снят блокер Задач 18 и 24: `is_shootouts` в БД верен |
| 30 | Пересобрать датасет на 5 сезонах ✅ | Выполнена (PR #24): датасет пересобран на всех сезонах, 6049 строк, `features_hash 09f1fac8…`, сброс rolling-окон на границе сезона | — | Снят единственный блокер трека B: 14, 15, 21 разблокированы |
| 14 | Параметры сплита ✅ | Выполнена (PR #26): guard `_check_minimum_history` вместо слепых нижних границ `SplitConfig`, smoke-профиль `configs/modeling_smoke.yaml` (50/50/3) рядом с рабочим 300/300/5 | — | Обучение на мультисезоне идёт по своему профилю; при малом N падает с числами, а не молча |
| 32 | Порядок блоков сплита ✅ | Выполнена (PR #27): `_assert_strictly_before` сравнивает позиции в ленте, а не календарные дни | — | Снят блокер Задачи 15: на многоматчевом календаре (895 дней, 6560 матчей) сплит собирается обоими профилями, до этого не собирался ни одним |
| 15 | Инференс-контур | Загрузка модели по `latest`, сверка `features_hash`, predict-датасет | 🟠 (разблокирована) | Инференс ни разу не прогонялся; блокирует 22, через неё 26. Учесть cold-start: в predict дефолтная `allow_with_flag` не отбрасывает первую игру сезона, и `validate.py` уронит сборку по NaN в rolling-фичах |
| 16 | Docstrings бота ✅ | Выполнена (PR #22): 43 публичные функции и 4 модульные шапки закрыты, охват проверен скриптом (0 пропусков в области) | — | Читаемость `telegram_bot/` восстановлена |
| 18 | «Спящие» данные ✅ | Выполнена (ветка `sdd-task-18-sleeping-data`, локальный мердж в master): хвост строки лидербордов команд/игроков/вратарей (`wins`-`losses`-`ot`/`points`, `games`/`shifts`, `games`/`saves`/`shots_against`/`time_on_ice[_per_game]`) в `bot_messages.py`, сортировка не менялась | — | Видимая польза пользователю из данных, за которые уже заплачено загрузкой |
| 19 | Миграции с rollback ✅ | Выполнена (PR #29): нумерованные `NNNN_slug.up/down.sql` + учёт в `schema_migrations`, `db-migrate`/`db-migrate-down` внутри `db-sync`/`db-reset`, первая миграция `0001_bot_subscriptions`, откат проверен на одноразовой БД | — | Изменение DDL стало обратимым. Предусловие FK закрыто: `extra_player_ids` объединяет advanced- и shottype-отчёты, глушение ошибки `landing` снято — отказ теперь виден сразу и до записи в БД |
| 20 | Hardening Docker ✅ | Выполнена (PR #28): non-root `USER` и `HEALTHCHECK` в `Dockerfile`, порт `db` на loopback | — | Бот больше не работает от root |
| 21 | Олимпийская пауза | Флаг разрыва для rolling-фич внутри сезона (74 игры, февраль 2026) | 🟡 | Сужена: механизм сброса окон пришёл из Задачи 30, здесь только разрыв внутри сезона |
| 22 | Прогноз в боте | Строка прогноза в карточке игры, только после acceptance-gate | 🟡 (после 15) | Единственная пользовательская отдача трека B; выпускать раньше gate нельзя — прогноз неотличим от base-rate |
| 23 | Три звезды матча ✅ | Выполнена (PR #31): таблица `game_three_stars`, третий пер-игровой эндпоинт `gamecenter/{id}/landing` (звёзд нет в `boxscore`) в диск-кэше Задачи 31, блок «Звёзды матча» в карточке игры бота | — | Прогон по всем 5 сезонам и проверка на живой БД выполнены координатором (6560 игр, 2026-09-15…09-18) — `game_three_stars` 19 670 строк, целостность и `make test-db-data` зелёные; кэш Задачи 31 заполнен полностью (132 МБ, 19 680 файлов), повторный прогон бесплатен |
| 31 | Кэш сырых пер-игровых ответов ✅ | Выполнена (PR #25): `play-by-play`/`boxscore` финальных игр кладутся на диск и читаются оттуда | — | Снят единственный блокер Задачи 23: второй прогон по проверочному окну — 0 запросов к `gamecenter/*` (было 112), ~14.0 КБ gzip на игру |
| 24 | Серии W/L | Вычисляемые фичи поверх формы команды | ⚪ (разблокирована) | Прогона **не** требует — считается поверх уже лежащих `games`. Осторожно с правилом NHL 84.2: «ПО» в таблице ≠ `games.is_overtime`, подробности в карточке задачи в `work_plan_2026-08-08.md` |
| 25 | Lock-файл + mypy | `uv`/`pip-tools` lock; сузить `ignore_missing_imports` | 🟡 | Сборка невоспроизводима: три `requirements*.txt` без пинов транзитивных зависимостей; mypy глобально слеп к типам библиотек |
| 26 | Retrain через cron | pipeline → train → gate → сдвиг `latest` | ⚪ (после 22) | Последнее звено трека B; до него весь цикл ручной |
| 27 | Чистка мёртвого кода | `close_pool`, `get_goal_video_url`, индекс `plan/README.md` | 🟡 | `plan/README.md` не упоминает ни одного `work_plan_*` — планы не находятся из индекса. Судьбу `close_pool` решает сама Задача 27 — Задача 16 (закрыта PR #22) её не предрешила |

## Порядок

Одна очередь, сверху вниз — так и брать. Из очереди ушли закрытые 13, 16, 29 и 30
(PR #20, #22, #23, #24), следом 31 (PR #25), 20 (PR #28), 19 (PR #29), трек B наполовину —
14 (PR #26) с 32 (PR #27), потом 23 (PR #31), и последней — 18 (ветка `sdd-task-18-sleeping-data`, локальный мердж). Следующая по
очереди — 15, вход в трек B.

| № | Задача | Почему здесь |
|---|---|---|
| 1 | **15** Инференс-контур | Вход в трек B: 30 дала датасет, 32 — собирающийся сплит. Область `modeling/` с Задачей 18 не пересекалась (та закрыта веткой `sdd-task-18-sleeping-data`) — можно вести параллельно |
| 2 | **21** Олимпийская пауза | Трек B, открыта закрытием 30 |
| 3 | **22** Прогноз в боте | Трек B, после 15 |
| 4 | **26** Retrain через cron | Трек B, последнее звено, после 22 |
| 5 | **27** Чистка мёртвого кода | Судьбу `close_pool` решает сама (Задача 16 закрыта PR #22, её не предрешив) |

Трек B (позиции 1–4) ни от чего в треке A не зависит — при желании его можно вести
параллельно, начиная с Задачи 15.

**Вне очереди — заполнять окна между крупными:** 25 (lock + mypy), 24 (серии W/L — блокер
снят Задачей 29). Задача 20 отсюда ушла — закрыта PR #28.

**Жёсткие зависимости сняты все:** 31 → 23 (PR #25), 30 → 14 (PR #24), 32 → 15 (PR #27).
Внутри трека B сохраняется порядок 15 → 22 → 26 (каждая следующая потребляет результат
предыдущей), 21 берётся в любой момент после 30. Остальное в таблице — приоритет,
а не блокировка: местами можно менять.

**Прогон загрузчика нужен ровно одной задаче — 23.** Не путать: 24 считается поверх уже
лежащих `games`, 18 выводит `shifts` из сезонного отчёта, уже загруженного в БД.

## Регламент

Не дублируется: обязательные команды, бюджет задачи, правила отметки о закрытии и
Global Constraints — в [`CLAUDE.md`](../../CLAUDE.md) и в шапке
[`work_plan_2026-08-08.md`](./work_plan_2026-08-08.md). Отметка о закрытии задачи ставится
**в исходном плане 2026-08-08** (три места: заголовок, строка статуса, сводка) и
дополнительно — в таблице этого файла.
