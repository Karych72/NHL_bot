# Refactoring Plan 2: Осталось сделать

**Статус:** в процессе (чеклист внутри файла). Сводка — [`README.md`](../README.md).

Документ — чеклист по [`refactoring_plan.md`](./refactoring_plan.md): отмечено **выполненное** (в т.ч. частично, с пояснением курсивом); без галочки — по-прежнему в работе или не начато.

## Фаза 1: Безопасность и критические баги

- [x] **1.2c / 5.1**: Объединить два `config.py` в единый конфиг-модуль (сейчас дублируются `_env()` и DB-настройки).
- [x] **1.3b**: Исправить баги в статистике игроков — *в `stats_handlers.py` фабрика использует колонки `plus_minus` и `pim`.*
- [x] **1.3c**: Исправить callback в `script_bot.py` для кнопки вратарей — *кнопка «Статистика вратарей» с `PLAYER_GOALIE` (`script_bot.py`).*
- [x] **1.3d**: Исправить шаблон `game_message.txt` — *в шаблоне используется `{{ goal['time'] }}`.*

## Фаза 2: Инфраструктура проекта

- [x] **2.3**: Добавить `Dockerfile` и `docker-compose.yml`.
- [x] **2.4**: Добавить GitHub Actions CI (линтинг, тайп-чекинг, тесты).

## Фаза 3: Качество кода

- [x] **3.1**: Перевести `print()` на `logging` (в т.ч. `bot.py`, `script_bot.py`, `load_season_modern.py`).
- [x] **3.2**: Убрать star-импорты в 6 файлах (`bot.py`, `script_bot.py`, `player_stats_functions.py`, `goalie_stats_functions.py`, `team_stats_functions.py`, `day_digest.py`).
- [x] **3.3b**: Удалить закомментированный код (`reply_markup`, `PG_PASSWORD`, `logging`, `argparse`).
- [x] **3.4b**: Добавить type hints в код бота (обработчики и функции).
- [x] **3.5**: Убрать copy-paste обработчики через фабрику хендлеров.

## Фаза 5: Архитектурные улучшения

- [ ] **5.4**: Добавить кэширование (`lru_cache`/`cachetools`) для часто запрашиваемых данных.
- [x] **5.5**: Досогласовать схему и код (например, `is_shootout` vs `is_shootouts`).

## Фаза 6: Тесты и документация

- [x] **6.1**: Добавить тесты — *частично: каталог `tests/` с unit-тестами; общего `conftest.py` пока нет; охват не полный.*
- [ ] **6.2b**: Добавить docstrings к публичным функциям.

## Фаза 7: UX и контент

- [ ] **7.1**: Вернуть `reply_markup` во всех обработчиках, чтобы не было тупика после показа статистики. *(основные потоки закрыты; см. «Отложено».)*
- [x] **7.2a**: Исправить переходы goalie-обработчиков: `TEAM_STATS` -> `CHOOSE_STATS`.
- [x] **7.2b**: Добавить в меню турнирную таблицу (`team_table()` уже есть).
- [x] **7.2c**: Добавить кнопку "Назад" на каждом уровне навигации. *(навигация игроки/полевые/расширенное; полный проход по всем веткам — в «Отложено».)*
- [x] **7.2d**: Добавить выбор даты дайджеста (Вчера / Сегодня / Выбрать дату) + «Назад» с ввода даты.
- [x] **7.3**: Обогатить сообщения — *частично: в карточке матча (`bot_messages.game_message`) — ББ/МБ/ПВ, периоды, позиция бомбардира, отметка победного гола (★); официальные три звезды матча NHL в выводе не задействованы.*
- [ ] **7.4**: Включить неиспользуемые данные (карточка команды, расширенная стата игроков/вратарей).
- [x] **7.5**: Добавить вычисляемые фичи (серии, хет-трики, сравнения) — *частично: в карточке матча и в /help пояснены форма (последние 5 игр, W–L–OTL) и хет-трик*.
- [x] **7.6**: Добавить push-уведомления (утренний дайджест, подписка на команду) — *таблица, opt-in/out, `push_digest_job.py`, `ENABLE_PUSH_DIGEST`*.
- [x] **7.7**: Перейти на HTML-форматирование и `<pre>` для таблиц — *частично: `/leaders`, лидерборды в /stats, карточка матча и сводка дайджеста на HTML; часть экранов остаётся Markdown*.
- [x] **7.8**: Добавить команды `/start`, `/standings`, `/today`, `/team`, `/help`. *(и расширенный набор: `/day_games`, `/tonight`, `/table`, `/leaders`, `/game`, `/advanced`, подписки.)*

### Отложено на следующие итерации

- Полное закрытие **7.1** и **7.2c** по всем сценариям `/stats`.
- Полный перевод всех пользовательских сообщений на HTML (**7.7**).
- Серии побед/поражений и прочие расширенные вычисляемые фичи (**7.5**) поверх текущих пояснений.
- **7.3**: вывести официальные три звезды матча NHL (сейчас в карточке — только отметка победного гола).
- **6.1**: `conftest.py`, интеграционные тесты и расширение охвата.

---

## Рекомендуемый порядок исправлений

1. **Сначала навигация (7.1 + добить 7.2c по всем веткам `/stats`)**
   - Пункты 1.3b–1.3d закрыты в коде.
   - Вернуть/дослать `reply_markup`, чтобы бот был полностью проходимым в UI.

2. **Качество и стабильность (остаток Фазы 6)**
   - Добести **6.1** (`conftest`, интеграционные сценарии) и **6.2b** (docstrings к публичному API бота).
   - Конфиг (**1.2c / 5.1**), согласование схемы (**5.5**), Docker/CI (**2.3–2.4**) и Фаза 3 уже закрыты в чеклисте выше.

3. **В конце продуктовые улучшения (Фаза 7 остальное + 5.4)**
   - Расширять UX/контент и добавить кэширование под часто запрашиваемые сценарии.
   - Это даст максимум пользы пользователю без риска сломать базовый поток.
