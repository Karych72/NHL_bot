# Сводный план работ — 2026-06-29

**Источники:** [`docs/project_review_2026-06-29.md`](../../docs/project_review_2026-06-29.md) (ревью + валидация по коду) и [`refactoring_plan_3.md`](./refactoring_plan_3.md).
**Назначение:** единый упорядоченный список работ на текущий момент с дедупликацией пересечений (review A1 ≈ DB-D/DB-C; review A3 ≈ 6.1b/6.1c).

---

## Примечания к приоритизации

- `refactoring_plan_3.md` датирован 2026-05-15 и ставит **7.1 (тупики в `/stats`)** как P0 — это **не перепроверялось в коде** ревью 2026-06-29. Перед тратой P0-усилий стоит подтвердить, что 7.1 всё ещё открыт (plan сам помечает «7.2 большей частью закрыто»).
- Треки **A (инженерный)** и **B (модельный)** независимы: при одном исполнителе порядок ниже линеен; при двух — B1 идёт параллельно треку A.

**Важность:** P0 критично → P3 потом. **Сложность:** 🟢 низкая · 🟡 средняя · 🔴 высокая.

---

## Таблица работ (по порядку)

| # | Задача | Короткое описание | Смысл / какую проблему решает | Важн. | Сложн. |
|---|--------|-------------------|-------------------------------|:----:|:----:|
| 1 | **7.1** Вернуть `reply_markup` во все ветки `/stats` | Клавиатура выхода/назад после любого действия в `/stats` | Убирает «тупик»: экран только с текстом без навигации | P0 | 🟢🟡 |
| 2 | **DB-в-CI** (A1 / DB-D + DB-C) | Postgres-job в GitHub Actions + включить `test_db_nhl.py`, цель `test-db-data` | Ловит ломающие изменения DDL/SQL автоматически, а не вручную | P0 | 🟡 |
| 3 | **A2** Апгрейд `python-telegram-bot` 13.15 → 21.x | Перевод хендлеров на async `Application`-API | Устраняет уязвимости 2021 г. и снимает блок на будущие фичи | P0 | 🔴 |
| 4 | **B1** Загрузить 3–5 прошлых сезонов | `season-sync` с историческими датами (~4–6k игр) | Без истории walk-forward неинформативен — блокер всего модельного трека | P0 | 🟡 |
| 5 | **7.2c** Полная навигация «Назад» во всех ветках `/stats` | Чеклист всех callback-веток интерактивного меню | Закрывает навигационные дыры в популярных сценариях | P1 | 🟡 |
| 6 | **5.4** Кэш read-only запросов бота | TTL-кэш таблицы/лидеров/справочников (`cachetools`) | Снижает нагрузку на PostgreSQL от частых запросов | P1 | 🟢🟡 |
| 7 | **A5** Rate-limiting в боте | Per-user троттлинг + debounce callback-кнопок | Гасит N+1 нагрузку от спама пагинацией | P1 | 🟢 |
| 8 | **6.1b + A3(бот)** Общий `conftest.py` + unit-покрытие | Тесты `database.py`, `stats_handlers`, сборки сообщений | Закрывает перекос: бот покрыт <30% против модели ~80% | P1 | 🟡 |
| 9 | **6.1c** Интеграционные тесты бота | Сценарий «запрос → ответ» с моками Telegram / тест-БД | Ловит регрессии навигации/форматов до больших UX-правок | P1 | 🟡🔴 |
| 10 | **A3(пайплайн)** Тесты загрузчика | Transform-хелперы и сборка строк на фикстурах NHL API | Покрывает пайплайн (<20%) — монолит 1543 стр. без тестов | P1 | 🟡 |
| 11 | **A4** Явные предупреждения при усечении | Маркер «показаны N из M» вместо тихой обрезки 4096/100 | Пользователь видит, что часть игр/данных скрыта | P1 | 🟢 |
| 12 | **A6** Валидация env на старте бота | Fail-fast при отсутствии `BOT_TOKEN`/`PG_*` | Падение на старте вместо рантайм-ошибки у пользователя | P1 | 🟢 |
| 13 | **B2** Параметры сплита под объём данных | Smoke-профиль + guard при N < минимума | Конфиг 300/300/5-окон не помещается на 1 сезоне | P1 | 🟢🟡 |
| 14 | **B4** Инференс-контур модели | Загрузка `latest`, сверка `features_hash`, заполнение predict-датасета | predict-датасет пуст — инференс технически не прогонялся | P1 | 🔴 |
| 15 | **6.2b** Docstrings публичных функций бота | Краткие docstrings: параметры, возврат, побочные эффекты | Улучшает поддерживаемость `telegram_bot/` | P2 | 🟢 |
| 16 | **7.7b** Полный перевод сообщений на HTML | Убрать смешанный Markdown/HTML `parse_mode` | Имена с `-`/спецсимволы перестают ломать разметку | P2 | 🟢🟡 |
| 17 | **7.4** Включить «спящие» данные | Карточка команды, расширенная стата игроков/вратарей | Раскрывает уже загружаемые поля БД в новых экранах | P2 | 🟡 |
| 18 | **A7** Миграции с rollback + индексы/FK | Alembic/нумерованные up/down, FK и индексы в DDL | Защищает от рассинхрона схемы и медленных запросов после B1 | P2 | 🟡🔴 |
| 19 | **A8** Hardening Docker / compose | Non-root, healthcheck, volume для PGDATA | Данные не теряются на `down`, образ безопаснее | P2 | 🟢🟡 |
| 20 | **B3** Обработка олимпийской паузы | Флаг разрыва для rolling-фич (фев-2026, 74 игры) | `shift(1)` через перерыв смешивает форму «до» и «после» | P2 | 🟡 |
| 21 | **B5** Интеграция прогноза в бота (Фаза 14) | Строка прогноза в карточке игры после прохождения gate | Текущий прогноз неотличим от base-rate — выпускать рано | P2 | 🟡 |
| 22 | **7.3b** Три звезды матча NHL | Вывод официальных трёх звёзд в карточке игры | Полнота контента карточки + fallback при отсутствии | P3 | 🟢 |
| 23 | **7.5b** Серии W/L и сравнения | Вычисляемые фичи поверх формы/хет-триков | Добавляет аналитику без дублирования `/help` | P3 | 🟢🟡 |
| 24 | **A9** Lock-файл зависимостей + ужесточить mypy | `uv`/`pip-tools` lock; сузить `ignore_missing_imports` | Воспроизводимые сборки + меньше скрытых ошибок типов | P3 | 🟢🟡 |
| 25 | **B6** Сезонный retrain через cron (этап 15) | Pipeline → train → gate → сдвиг симлинка `latest` | Автообновление модели после стабилизации на мультисезоне | P3 | 🟡 |
| 26 | **DC-2** Решение по `close_pool()` | Удалить + правка `architecture.md` или подключить graceful shutdown | Убирает dead code / закрывает утечку при остановке | P3 | 🟢 |
| 27 | **DC-3** Решение по `get_goal_video_url()` | Удалить как неиспользуемую или задействовать в `send_video` | Убирает мёртвый код | P3 | 🟢 |
| 28 | **DC-meta** Актуализировать индекс планов | `make test-fast`, обновить `plan/README.md`, заархивировать чеклисты | Планы не ссылаются на выполненные чеклисты как на активные | P3 | 🟢 |

---

## Сведённые расхождения источников

- **DB-в-CI:** ревью ставит **P0**, `refactoring_plan_3.md` — **P1**. Принято **P0** (закрывает две дыры сразу: DB-тесты не в CI + ломкость DDL).
- **A2 (PTB-апгрейд)** и весь **B-трек** отсутствуют в `refactoring_plan_3.md` — добавлены из ревью.
- **A5 (rate-limit)** и **5.4 (кэш)** частично пересекаются — оставлены отдельными строками (троттлинг ≠ кэширование).

## Снято по итогам валидации ревью

- **Атомарность DELETE→INSERT** (был «критичный» пункт ревью) — в плане отсутствует: `load_season_modern.py:1149–1490` уже оборачивает операцию в одну транзакцию (`autocommit=False` → единственный `commit()` → `rollback()` в `except`). Подробности — в `docs/project_review_2026-06-29.md`.
