target-version = "py311"
line-length = 120

# ``load_season_modern`` imports shared ``telegram_bot.config`` after ``sys.path`` fix-up.
[lint.per-file-ignores]
"pipeline/load_season_modern.py" = ["E402"]
