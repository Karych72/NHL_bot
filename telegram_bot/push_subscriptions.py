"""Дизайн push-уведомлений (фаза 7.6): утренний дайджест и подписка на команду.

Модель данных (рекомендуемый минимум перед реализацией рассылки)
-----------------------------------------------------------------
Таблица ``bot_subscriptions`` (PostgreSQL рядом с остальной БД бота), колонки::

    chat_id        BIGINT NOT NULL,       -- Telegram chat_id получателя
    kind           TEXT NOT NULL,         -- 'morning_digest' | 'team_scores'
    team_id        BIGINT,                -- для kind='team_scores'; NULL для дайджеста
    timezone       TEXT,                  -- опционально IANA, для окна «утра»
    active         BOOLEAN DEFAULT TRUE,
    created_at     TIMESTAMPTZ DEFAULT now()

Уникальность строки: пара (chat_id, kind) для дайджеста; для команды —
(chat_id, kind, team_id). Реализуйте через UNIQUE-индекс или составной PRIMARY KEY.

Хранить нужно минимум ``chat_id``: без активного диалога пользователь не получит
сообщение, если бот не писал ему после последнего рестарта — поэтому подписка
предполагает, что пользователь хотя бы раз нажал /start или открыл меню.

Ограничения Telegram Bot API
-----------------------------
- Сообщения в личку возможны только если пользователь начал чат и не заблокировал бота.
- Лимиты отправки (~30 сообщений/сек на бота; единые лимиты на группы) — рассылку
  нужно чанковать и при ошибках ``429`` делать backoff.
- Нельзя инициировать диалог первым без предварительного opt-in; текст кнопки
  «Подписаться» + сохранение chat_id считается явным согласием.

Утренний дайджест
-----------------
Внешний планировщик (cron, systemd timer, облачная задача) вызывает скрипт,
который для всех ``active`` строк с ``kind='morning_digest'`` вызывает ту же
логику, что и ``day_digest()`` / ``dispatch_day_digest_messages``, и шлёт
результат с ``attach_conv_nav_on_last=False`` и без callback-кнопок диалога.

Подписка на команду
-------------------
После игры команды (или по расписанию раз в день) слать краткое сообщение
подписчикам с ``kind='team_scores'`` и совпадающим ``team_id``.

Реализация: таблица ``scripts/create_bot_subscriptions.sql``, цель ``make db-bot-subscriptions``,
слой данных ``subscription_repo.py``, рассылка ``push_digest_job.py`` (батчи, паузы, ``429``,
``Forbidden``), команды в ``bot.py`` (см. ``docs/telegram_bot.md`` §7).
"""
