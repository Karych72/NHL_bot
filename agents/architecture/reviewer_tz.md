# ТЗ для ревьюера: Фаза 5 — архитектурные улучшения

**Роль:** независимый reviewer.  
**Источник:** `plan/engineering/refactoring_plan_2.md`, Фаза 5. Пара ТЗ: [`executor_tz.md`](executor_tz.md).

---

## 1. Принцип ревью

Главный риск **5.4** — **устаревшие данные**, **раздувание памяти** и кэширование **неподходящих** функций (побочные эффекты, мутабельные аргументы).

---

## 2. Проверка **5.4**

- [ ] Выбор `lru_cache` vs `cachetools` обоснован типом данных и жизненным циклом.
- [ ] Для TTL-кэша заданы разумные лимиты; нет неограниченных dict без eviction.
- [ ] Кэш не скрывает ошибки БД (не отдаёт закэшированную ошибку бесконечно без политики).
- [ ] Сезон/дата/идентификатор матча учтены в ключе там, где устаревание критично.
- [ ] Нет гонок и глобального состояния без необходимости (при асинхронности — отдельно проверить thread/async safety выбранного backend).

---

## 3. Регресс **5.5**

Быстрая выборочная проверка: код и схема по-прежнему согласованы (`is_shootout`/`is_shootouts`), нет возврата к старому багу.

---

## 4. Вердикт

**approve / approve with nits / request changes** + блокеры по корректности данных и утечкам.
