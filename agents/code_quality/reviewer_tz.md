# ТЗ для ревьюера: Фаза 3 — качество кода

**Роль:** независимый reviewer.  
**Источник:** `plan/engineering/refactoring_plan_2.md`, Фаза 3. Пара ТЗ: [`executor_tz.md`](executor_tz.md).

---

## 1. Принцип ревью

Фаза заявлена **закрытой** в плане. Ревью — это **аудит на ложноположительное закрытие**: поиск вернувшихся star-imports, `print`, закомментированного мусора, отката фабрики хендлеров.

---

## 2. Обязательные проверки

- [ ] Нет star-imports в файлах из плана 3.2.
- [ ] Логирование: критические пути не полагаются на `print` для диагностики.
- [ ] Type hints не «обманывают» (Any везде, игнор комментарии без причины).
- [ ] Обработчики: дубли сведены к общему паттерну там, где это было требованием (нет очевидного copy-paste там, где план требовал обратного).
- [ ] Мёртвый код из 3.3b не восстановлен.

---

## 3. Процесс

1. Сверить diff с чеклистом; отдельно просмотреть **hot paths** бота.
2. Убедиться, что CI (если есть) действительно ловит нарушения, а не только «зелёный» на пустом прогоне.

---

## 4. Вердикт

**approve / approve with nits / request changes**. Если найдены расхождения с планом — указать конкретные файлы/строки и рекомендацию: исправить код или **явно** пересмотреть статус чеклиста в плане.
